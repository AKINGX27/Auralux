# Auralux Tauri Shell

This shell reuses `apps/gui` for desktop and Android. The first version keeps media compute in the local daemon on desktop. Android native libmpv/FFmpeg integration belongs in a follow-up Tauri plugin that owns MediaSession, audio focus, notification transport controls, and ABI split codec packaging.

